# Backend API URL (required for production)
# For local dev leave empty to use relative paths (proxy in vite.config)
VITE_API_URL=

# Example for production:
# VITE_API_URL=https://your-backend-url.com
